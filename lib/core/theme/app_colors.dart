import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF0A1929);
  static const Color primaryLight = Color(0xFF1A2939);
  static const Color primaryDark = Color(0xFF050D14);

  // Accent Colors
  static const Color accent = Color(0xFF2196F3);
  static const Color accentLight = Color(0xFF64B5F6);
  static const Color accentDark = Color(0xFF1976D2);

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textLight = Color(0xFFFFFFFF);

  // Background Colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFFF5F5F5);
  static const Color backgroundLight = Color(0xFFFAFAFA);

  // Error Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFF57C00);
  static const Color info = Color(0xFF1976D2);

  // Secondary colors
  static const Color secondaryColor = Color(0xFFF7C948);  // Gold/Yellow
  
  // Background colors
  static const Color lightGrayBackground = Color(0xFFF5F7FA);
  
  // Card colors
  static const Color cardBackground = Colors.white;
  static const Color cardShadow = Color(0x1A000000);
  
  // Button colors
  static const Color buttonPrimary = Color(0xFF0C1A2E);
  static const Color buttonSecondary = Colors.white;
  
  // Status tag colors
  static const Color currentAthleteBackground = Color(0xFFDEEBFF);
  static const Color currentAthleteText = Color(0xFF0958D9);
  static const Color formerAthleteBackground = Color(0xFFDCF7E3);
  static const Color formerAthleteText = Color(0xFF17AD49);

  // Gradient colors
  static const List<Color> blueGradient = [Color(0xFF3B82F6), Color(0xFF1E40AF)];
  static const List<Color> purpleGradient = [Color(0xFFA855F7), Color(0xFF6B21A8)];
  static const List<Color> amberGradient = [Color(0xFFFCD34D), Color(0xFFD97706)];

  // Grid background
  static const Color gridLine = Color(0xFF1E3A8A);
  static const Color gridBackground = Color(0xFF0C1A2E);
  
  // Input field colors
  static const Color inputBackground = Color(0xFFF9FAFB);
  static const Color inputBorder = Color(0xFFE5E7EB);
  
  // Social buttons
  static const Color googleButtonBackground = Colors.white;
  static const Color linkedinButtonBackground = Color(0xFF0A66C2);
}